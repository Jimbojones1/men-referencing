<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= listing.name %>
  </title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>

    <h1>A listing in <%= listing.city %>
    </h1>

    <h2>Details</h2>
    <p>House Size: <%= listing.size %> sq ft</p>
    <p>Price: $<%= listing.price %>
    </p>
    <p>Street address: <%= listing.streetAddress %>
    </p>
    <!-- the .equals compars mongo ids, this is from mongoose -->
    <% if (listing.owner._id.equals(user._id)) { %>

      <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
      </form>
    <% } %>

    <h2>Favorites</h2>
    <p>Favorited by <%= listing.favoritedByUsers.length %> people.</p>

    <% if (isFavoritedByUser) { %>
     <p>You have favorited this listing</p>
    <% } else { %>
      <form action="/listings/<%= listing._id %>/favorited-by" method="POST">
        <button type="Submit">Favorite it!</button>
      </form>
    <% } %>
 

</body>

</html>